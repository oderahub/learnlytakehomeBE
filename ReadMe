# Project Name

## Description

A brief description of your project, what it does, and why it's useful. This project is a Node.js application using TypeScript, Sequelize, and PostgreSQL (Supabase).

## Features

- User management with authentication
- CRUD operations for products
- Swagger API documentation
- TypeScript and Sequelize integration
- Custom scripts for running migrations

## Installation

1. **Clone the repository:**

   ```bash
   git clone 

##  Navigate into the project directory:

```bash
cd your-repo
Install dependencies:

```bash
yarn install
Create a .env file based on the .env.example file and set your environment variables.

Run migrations:

bash

yarn migrate

Usage
Start the development server:

bash

yarn start



Run TypeScript compiler:

bash

yarn tsc
